# ===== Firebase Auth (Client-side) =====
FIREBASE_WEB_API_KEY=YOUR_FIREBASE_WEB_API_KEY
FIREBASE_DATABASE_URL=https://YOUR_PROJECT_ID-default-rtdb.firebaseio.com

# ===== Signals =====
GLOBAL_SIGNALS_EVENTS_PATH=global_council_signal_events
GLOBAL_SIGNALS_LATEST_PATH=global_council_signals_latest

# ===== Local bot storage =====
BOT_DATA_DIR=/data

# ===== Execution =====
EXECUTION_MODE=paper           # paper|live|dry_run
LOG_LEVEL=INFO

# Broker endpoints (non-secret)
ALPACA_TRADING_BASE_URL=https://paper-api.alpaca.markets
ALPACA_DATA_BASE_URL=https://data.alpaca.markets
ALPACA_DATA_FEED=iex

# Broker API keys:
# Preferred: provision from mobile app via encrypted command channel (SET_BROKER_KEYS).
# Optional fallback (backward compatible):
# ALPACA_API_KEY=
# ALPACA_SECRET_KEY=

# ===== Bot tuning / cadence =====
TICK_SECONDS=10
CONFIG_REFRESH_SECONDS=15
STATUS_WRITE_SECONDS=10
TELEMETRY_WRITE_SECONDS=10
COMMAND_POLL_SECONDS=5

# Pairing / device control
PAIRING_TTL_SECONDS=900

# Hygiene (optional; requires bot build that supports pruning)
ACKS_KEEP_LAST=250
LOGS_KEEP_LAST=500
RTDB_PRUNE_INTERVAL_S=600
